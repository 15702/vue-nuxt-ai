`app/components`：展示层组件（Navbar/Hero/AboutSection/ChatSection）。
`Navbar.vue`：sticky + 毛玻璃导航；`Hero.vue`：头像/标题/状态；`AboutSection.vue`：关于我卡片（2x2/单列响应式）。
`ChatSection.vue`：数字分身聊天 UI（消息气泡/打字指示器/快捷提问/输入发送），业务逻辑在 composables。
